spring:
  application:
    name: api

  datasource:
    url: "jdbc:postgresql://localhost:5433/travel_planner"
    username: "postgres"
    password: "postgres123"
    driver-class-name: "org.postgresql.Driver"

  jpa:
    hibernate:
      ddl-auto: "update"
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: "org.hibernate.dialect.PostgreSQLDialect"